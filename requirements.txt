# ============================================================
# Ultimate Trading Bot v2 — requirements.txt (Python 3.10)
# ============================================================

# --- Web API / Panel (FastAPI) ---
fastapi>=0.110,<1.0        # Python>=3.9 3
uvicorn>=0.25,<1.0
jinja2>=3.1,<4.0
python-multipart>=0.0.9,<1.0

# --- Config / Settings ---
pydantic>=2.6,<3.0
pydantic-settings>=2.0,<3.0  # Requires Python>=3.10 4
python-dotenv>=1.0,<2.0
pyyaml>=6.0,<7.0

# --- HTTP / Utilities ---
requests>=2.31,<3.0
httpx>=0.27,<1.0

# --- Alpaca (Trading + Market Data) ---
alpaca-py>=0.20,<1.0       # Python 3.8+ 5

# --- Data / Compute ---
numpy>=1.24,<3.0
pandas>=2.0,<3.0           # Python 3.10 supported 6
pyarrow>=15.0,<23.0        # Python 3.10 compatible 7

# --- ML / Optimization (MLOps-lite) ---
scipy>=1.10,<2.0
scikit-learn>=1.7.0,<1.8.0 # 1.7 requires Python>=3.10 8
optuna>=3.5,<5.0           # Python 3.9+ 9
joblib>=1.3,<2.0

# --- Reporting / Charts (optional but useful for panel) ---
plotly>=5.18,<6.0

# --- Build (Windows EXE) ---
pyinstaller>=6.0,<7.0

# --- Tests ---
pytest>=7.4,<9.0
pytest-asyncio>=0.23,<1.0
